/**
 * Placeholder so the freshly-scaffolded package typechecks before the `lib/game`
 * move populates `src/{foundation,data,engine}` (docs/engine-reorg). Delete once
 * the first real module lands.
 */
export {}
